{% extends 'base.html.twig' %}

{% block title %}Page{% endblock %}

{% block body %}

<div class="container">
    <div class='h2 mb-2'> {{ page.project.fullName}} </div>
    <div {{ vue_component('MdPageComponent', { 'pageData': pageData, 'projectIdData': projectId} ) }}></div>
    
    {# <a href="{{ path('app_page_index') }}">back to list</a> #}

    {# <a href="{{ path('app_page_edit', {'id': page.id}) }}">edit</a> #}

    {{ include('page/_delete_form.html.twig') }}
    </div>
{% endblock %}
