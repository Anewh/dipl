{% extends 'base.html.twig' %}

{% block title %}Field index{% endblock %}

{% block body %}
    <h1>Field index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Header</th>
                <th>Content</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for field in fields %}
            <tr>
                <td>{{ field.id }}</td>
                <td>{{ field.header }}</td>
                <td>{{ field.content }}</td>
                <td>{{ field.type }}</td>
                <td>
                    <a href="{{ path('app_field_show', {'id': field.id}) }}">show</a>
                    <a href="{{ path('app_field_edit', {'id': field.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_field_new') }}">Create new</a>
{% endblock %}
