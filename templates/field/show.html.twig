{% extends 'base.html.twig' %}

{% block title %}Field{% endblock %}

{% block body %}
    <h1>Field</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field.id }}</td>
            </tr>
            <tr>
                <th>Header</th>
                <td>{{ field.header }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ field.content }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ field.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_field_index') }}">back to list</a>

    <a href="{{ path('app_field_edit', {'id': field.id}) }}">edit</a>

    {{ include('field/_delete_form.html.twig') }}
{% endblock %}
